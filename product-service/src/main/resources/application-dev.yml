server:
  port: 8110

# 스프링 설정
spring:
  devtools:
    livereload:
      enabled: true

  # 데이터베이스 세팅
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://greenmarket_mysql:3306/green_market
    username: root
    password: siru@dev1

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

# 업로드 파일 설정
resource:
  file:
    path: /resources/images/product/

# Another Micro Service Config
service:
  chat:
    host: host.docker.internal
    port: 8140
  user:
    host: host.docker.internal
    port: 8130

# mybatis
mybatis:
  mapper-locations: classpath:mybatis/mapper/**/**.xml


logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace